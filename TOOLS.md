# Nexus Tool Inventory

_Last generated: 2026-04-29T17:54:42-07:00_
_Total: 92 tools across 16 categories_

This file is auto-generated by `scripts/generate_tools_md.py`. Don't hand-edit — run the generator instead. Refreshed nightly by `nexus-tools-refresh.timer`.

## Web & Search

- `brave_search(query, count)` — Search the web via the Brave Search API.
- `brave_search_news(query, count)` — Search news articles specifically via Brave Search News API.
- `browser_tool(url)` — Open `url` in a headless Chromium browser and return the visible page
- `markitdown_tool(source)` — Convert a document (PDF, Word, Excel, PowerPoint, HTML, image, audio,
- `youtube_summary(url)` — Extract transcript from YouTube video and summarize it using qwen3:4b.
- `youtube_transcript(url)` — Extract full transcript from a YouTube video.

## GitHub

_Auth: token resolved from `~/AI_Agent/config/secrets.yaml` (`GITHUB_PAT`, fine-grained — preferred), then env vars, then `~/AI_Agent/.env` (`GITHUB_TOKEN`, classic). When no token is configured, falls back to anonymous PyGithub — **public repos only, ~60 req/h rate limit**. Run `github_auth_status()` to see who you're logged in as, what scopes the token has, and the current rate-limit budget._

- `github_auth_status()` — Report GitHub auth state: who, what scopes, rate limit, expiry.
- `github_commit_file(repo, path, content, message, branch, approve)` — Create or update a single file in a GitHub repo on the given branch.
- `github_create_issue(repo, title, body)` — Open a new issue on a repo.
- `github_create_pr(repo, title, head, base, body)` — Open a pull request on a repo.
- `github_create_repo(name, private, description)` — Create a new repository under the authenticated GitHub user.
- `github_get_file(repo, path, ref)` — Read a file from a GitHub repo.
- `github_list_issues(repo, state, limit)` — List issues on a repo.
- `github_list_my_repos(visibility, limit)` — List ALL repos the authenticated GitHub user can access (public + private).
- `github_list_repos(visibility, limit)` — List the authenticated user's repositories.

## File System

- `file_edit_tool(path, old_string, new_string)` — Find-and-replace in a file. Replaces every occurrence of old_string
- `file_read_tool(path)` — Read a file from disk and return its contents.
- `file_write_tool(path, content)` — Write (or overwrite) a file with the given content.
- `glob_tool(pattern, root)` — List files matching a glob pattern under `root`. Pattern supports ** for
- `grep_tool(pattern, root, glob)` — Search for a regex `pattern` across files under `root` matching `glob`.

## System & Shell

- `terminal(command)` — Run a shell command on the host (bash, 60s hard timeout).

## Code & Dev

- `approve_diff(repo_path, message)` — If `review_diff` returns without concerns, stage and commit with
- `get_diff(repo_path)` — Return the current git diff for `repo_path` (staged + unstaged).
- `get_file_context(file_path)` — Return the file's content plus the list of repo files that import
- `index_codebase(repo_path)` — Deep-scan a git repo: read every tracked source file, extract
- `list_repo_structure(repo_path, max_depth)` — Return a clean, indented tree of the repo (git-tracked files only
- `review_diff(repo_path)` — Send the current diff to qwen3.6 for a senior-engineer review.
- `run_specific_test(repo_path, test_name)` — Run a single test by name. Builds a pytest / jest command matching
- `run_tests(repo_path, test_command)` — Auto-detect the test framework for a repo and run its tests.
- `search_codebase(query, repo_path, k)` — Semantic search across indexed codebases. If `repo_path` is given,
- `solve_task(task, repo_path, max_iterations)` — Autonomous coding loop. Indexes a git repo, plans the task with
- `watch_tests(repo_path, max_seconds)` — Run tests in watch mode for up to `max_seconds` seconds. Useful for

## Memory & RAG

- `mem0_add(text)` — Add a memory to Mem0. Mem0 uses the local LLM (qwen3:4b) to extract
- `mem0_search(query, k)` — Semantic search over Mem0. Returns up to k extracted memories
- `memory_add(text)` — Store a text snippet in Nexus's long-term memory for future recall.
- `memory_compact()` — Compact the Chroma database to reclaim space after deletions.
- `memory_dedup(threshold, dry_run)` — Find and optionally remove near-duplicate documents from memory.
- `memory_delete(doc_id)` — Delete a document from Nexus's long-term memory by ID.
- `memory_list(tag, limit)` — List documents in Nexus's long-term memory.
- `memory_search(query_text, k)` — Search Nexus's long-term memory for passages similar to query_text.
- `memory_stats()` — Get statistics about Nexus's long-term memory.

## Computer Use (mouse/keyboard/screen)

- `find_on_screen(image_path)` — Find an image on the screen and return its coordinates.
- `find_on_screen_vision(description, vision_model)` — Locate a UI element on screen by *natural-language description* using
- `get_mouse_position()` — Get the current mouse cursor position.
- `get_screen_size()` — Get the current screen resolution.
- `keyboard_press(key)` — Press a keyboard key or key combination.
- `keyboard_type(text)` — Type text using the keyboard.
- `mouse_click(x, y, button, approve)` — Click the mouse at the specified coordinates.
- `mouse_drag(x1, y1, x2, y2, button)` — Drag the mouse from one position to another.
- `mouse_move(x, y)` — Move the mouse cursor to the specified screen coordinates.
- `open_app(name)` — Open an application by name (Linux only).
- `screenshot(save_to_file)` — Take a screenshot of the current screen.

## Audio & Speech

- `bark_list_presets()` — List available voice presets for Bark.
- `bark_list_voices(limit)` — List recently generated voice audio files.
- `bark_speak(text, voice_preset, filename)` — Generate voice acting audio from text using Bark.
- `generate_music(prompt, duration, filename)` — Generate background music from a text prompt.
- `generate_sfx(prompt, duration, filename)` — Generate a sound effect from a text prompt.
- `list_audio_files(limit)` — List recently generated audio files.
- `tts_save(text, path, voice)` — Synthesize `text` to a WAV file at `path`. Voice defaults to
- `tts_speak(text, voice)` — Speak `text` out loud through the default audio device. Voice
- `whisper_record(max_seconds)` — Record audio from the default microphone and transcribe it with
- `whisper_transcribe(path)` — Transcribe an existing audio file (wav / mp3 / m4a / flac / ogg)

## Image & Game

- `create_game(prompt, game_name)` — Create a complete game from a text prompt using the full pipeline.
- `generate_image(prompt, size, style, filename)` — Generate an image from a text prompt.
- `godot_create_project(name, template)` — Create a new Godot project.
- `godot_run_export(project_dir, platform)` — Export a Godot project for a specific platform.
- `godot_run_headless(project_dir)` — Run a Godot project in headless mode for testing.
- `list_created_games(limit)` — List games created by the pipeline.
- `list_generated_images(limit)` — List recently generated images.
- `opengame_create(prompt, game_name)` — Generate a complete playable web game from a text prompt using OpenGame.
- `opengame_list_games(limit)` — List recently generated games.

## Knowledge Sync

- `chatgpt_history_import(json_path)` — Import a ChatGPT conversation export JSON into RAG (tag='chatgpt_history').
- `claude_history_import(json_path)` — Import a Claude conversation export JSON into RAG (tag='claude_history').
- `notion_sync(database_id, limit)` — Pull pages from a Notion database and seed them into RAG (tag='notion').
- `obsidian_sync(root)` — Index every .md file under ~/Obsidian (or the given root) into RAG.

## Deployment

- `vercel_deploy(project_dir, project_name, prod, approve)` — Deploy a project to Vercel.
- `vercel_list_deployments(limit)` — List recent Vercel deployments.
- `vercel_remove_deployment(url)` — Remove a Vercel deployment.

## Notifications

- `telegram_notify(message)` — Send a notification message to Colton's Telegram.
- `telegram_send_file(file_path, caption)` — Send a file to Colton's Telegram with an optional caption.

## BidWatt (read-only)

- `bidwatt_get_bid(bid_id)` — Fetch one BidWatt bid by id (read-only).
- `bidwatt_list_bids(limit, status)` — List recent BidWatt bids (read-only).
- `bidwatt_search_bids(query, limit)` — Search BidWatt bids by free-text against project_name and client (read-only).

## External Models (escalation)

- `glm_consult(prompt, reason, model)` — Send `prompt` to GLM (Z.ai) for a second-opinion answer when local

## Meta & Telemetry

- `list_capabilities()` — Return Nexus's full tool inventory (TOOLS.md content).
- `model_watcher_run()` — Compare local Ollama models to the public library and report new
- `quick_lookup(query, count)` — Run web search and RAG memory search in parallel for one query.
- `repo_inspect(file_path, repo_path, git_n)` — Read a file's symbol/import context AND the repo's recent git log in parallel.
- `router_stats()` — Quick summary of router statistics (last 7 days).
- `router_telemetry(days)` — Generate a router telemetry dashboard from the run log.
- `screen_clip()` — Capture clipboard contents AND a fresh screenshot in parallel.

## Other

- `browser_render(url, wait_for_selector, timeout)` — Render a JS-heavy URL with headless Chromium and return visible text.
